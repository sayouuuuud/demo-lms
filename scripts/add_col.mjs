import pkg from 'pg'; const { Client } = pkg; const client = new Client({ connectionString: process.env.DATABASE_URL }); async function run() { await client.connect(); try { await client.query('ALTER TABLE lectures ADD COLUMN IF NOT EXISTS what_you_learn text[] DEFAULT ''{}'';'); console.log('Successfully added what_you_learn column'); } catch (e) { console.error('Error:', e); } finally { await client.end(); } } run();  
